require 'spec_helper'

describe FakerPipe do
  it 'has a version number' do
    expect(FakerPipe::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
